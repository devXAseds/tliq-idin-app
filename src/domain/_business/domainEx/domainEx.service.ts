import { DomainInterface } from "@mahdi/mahdi-lib/interfaces/domain.interface";

export class DomainExService {
    async createDomainEx(domain: DomainInterface): Promise<Result<DomainInterface | Result<Error>> {

    }
}